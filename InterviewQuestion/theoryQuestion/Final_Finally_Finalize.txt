---final is a keyword used for constants, methods(no overriding), and classes(no extend).

---finally is a block used in exception handling to ensure a piece of code is always executed.

---finalize is a method in the Object class used for resource cleanup before garbage collection
   (though it's generally not recommended for that purpose).